fastapi
uvicorn[standard]
python-dotenv
sqlalchemy
supabase
numpy
Pillow
passlib[bcrypt]
python-jose[cryptography]
pydantic[email]
psycopg2-binary
python-multipart

# Note: `face-recognition` is optional and requires native system libraries.
# If you deploy with Docker (recommended for face recognition), use
# `requirements.docker.txt` which includes the native package.
